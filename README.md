# halyk-agent

Агент для Halyk AI Challenge. Читает корпус обезличенных финансовых PDF и реестр транзакций и
по каждому финансовому ковенанту каждого заёмщика определяет три вещи:

| Поле | Что это |
|---|---|
| `status` | `COMPLIANT` или `BREACH` |
| `actual` | фактическое значение показателя, который ограничивает ковенант |
| `evidence_txn_id` | единственная транзакция, определяющая результат, или `null` |

Результат — `out/submission.json` по форме `data/submission_template.json`.

## Запуск

Сборка и полный прогон — две команды. Что должно лежать в `data/`, что нужно установить и что
задать в `.env`, описано ниже: [вход](#что-нужно-на-входе), [машина](#что-нужно-на-машине),
[настройка](#настройка).

```bash
go build -o bin/halyk-agent ./cmd/halyk-agent

bin/halyk-agent llm-check    # проверить ключ, модель и обвязку провайдера одним запросом
bin/halyk-agent run          # весь конвейер: ingest → … → report
```

`run` доводит прогон до `out/submission.json` при любом исходе и печатает список деградировавших
стадий; код возврата ненулевой, если деградировала хоть одна.

Стадии по отдельности — каждая продолжает с того, что оставила предыдущая:

```bash
bin/halyk-agent ingest      # фаза 0 — леджер, шаблон, текст всех PDF (без LLM)
bin/halyk-agent triage      # фаза 1 — тип документа и его заёмщик
bin/halyk-agent covenants   # фаза 2 — CovenantSpec на каждый пункт + критик
bin/halyk-agent facts       # фаза 3 — корректировки аудитора, стороны, курсы
bin/halyk-agent classify    # фаза 4 — категория и связанность на каждой строке
bin/halyk-agent evaluate    # фазы 5-6 — движок, evidence, критик по ячейкам
bin/halyk-agent submit      # фаза 7 — заполнение шаблона
bin/halyk-agent report      # фаза 8 — out/report.pdf (без LLM)
```

Вспомогательные команды:

```bash
bin/halyk-agent specs P1     # спеки заёмщика рядом с текстом пункта — для ручной сверки
bin/halyk-agent labels P1    # размеченный леджер: итоги по категориям и все строки
bin/halyk-agent validate     # проверка submission против шаблона
bin/halyk-agent score        # локальный скоринг против ground_truth.json (только отладка)
bin/halyk-agent stability    # сколько ячеек держится при повторных прогонах модели
bin/halyk-agent status       # что уже лежит в сторе
bin/halyk-agent version      # сведения о сборке
```

Состояние живёт в SQLite (`.cache/halyk.db`), ответы модели кэшируются по хешу промпта. Правка
фазы 5 не требует повторного прогона фаз 1–4, а запуск после обрыва доигрывает по кэшу и не
тратит квоту заново. Изменение текста промпта кэш обнуляет — стадия пойдёт в сеть.

## Архитектурный принцип

**LLM извлекает и классифицирует. Go считает и решает.**

Ни один агент не возвращает вердикт. Агенты производят три вида структурированных артефактов:

| Артефакт | Что это | Фаза |
|---|---|---|
| `CovenantSpec` | исполняемая спецификация пункта: арифметическое выражение над именованными термами, порог, оператор, период, carve-out'ы, триггер, статус контрагентов | 2 |
| `FactBase` | корректировки аудитора, доли владения и порог связанности, курсы валют, показатели группы | 3 |
| `Label` | категория формулировки по таксономии и флаг связанной стороны на строке | 4 |

Дальше детерминированный движок на Go применяет спеки к размеченным данным: считает метрику в
`decimal`, проверяет триггер, применяет оговорки, сравнивает с порогом и выбирает evidence
методом leave-one-out. Модель никогда не складывает числа.

Причина в схеме оценки: ошибка в `status` обнуляет ячейку целиком, а `actual` оценивается по
линейной шкале с нулём при 5% отклонения. Арифметика в LLM — самый дешёвый способ потерять всё.

## Что нужно на входе

Датасет кладётся в `./data` (или в каталог из `DATA_DIR`):

```
data/
├── documents/                    все PDF датасета, имена — непрозрачные хеши
│   ├── 04eee2e9ba8c.pdf
│   └── ...
├── master_ledger_2025.csv        реестр транзакций по всем заёмщикам, одна таблица
├── submission_template.json      список сценариев и пунктов с пустыми ячейками
└── ground_truth.json             необязательно: ключ для локального скоринга
```

Первые три обязательны — без них стадия останавливается и называет, чего не хватает.
`ground_truth.json` нужен только команде `score` и в решении не участвует.

Колонки леджера: `txn_id`, `date`, `account_id`, `counterparty`, `description`, `amount`,
`currency`. Расходы отрицательные, поступления положительные, часть сумм может быть пустой.
Заёмщика строки задаёт префикс идентификатора: `TXN-P1-0039` принадлежит сценарию `P1`.

**Документы раскладывать по папкам не нужно и переименовывать нельзя.** Все PDF лежат одной
кучей в `data/documents/`; что это за документ и к какому заёмщику он относится, агент
определяет сам на фазе 1 — по содержимому, а не по имени файла.

## Что нужно на машине

- **Go 1.26+** — сборка.
- **poppler** (`pdftotext`, `pdftoppm`) — текстовый слой PDF и рендер страниц.
- **tesseract** с языками `rus`, `eng`, `kaz` — страницы без текстового слоя читаются только OCR.
- **Ключ LLM** — нужен стадиям 1–6. Провайдер по умолчанию — OpenAI-совместимый эндпоинт Gemini.

SQLite вкомпилирован (`modernc.org/sqlite`), внешней СУБД не требуется. Шрифты для отчёта вшиты
в бинарник.

## Настройка

Параметры читаются из `.env` в рабочем каталоге. Уже экспортированную переменную окружения файл
не перекрывает, поэтому случайный `export MODEL=...` в оболочке имеет приоритет.

```bash
LLM_API_KEY=...
TEAM=your-team-name
CONTACT_EMAIL=you@example.com
MODEL=gemini-3.5-flash-lite
```

| Переменная | Назначение | По умолчанию |
|---|---|---|
| `LLM_API_KEY` | ключ провайдера; читается и `GOOGLE_API_KEY` | — |
| `LLM_BASE_URL` | корень OpenAI-совместимого API | `https://generativelanguage.googleapis.com/v1beta/openai` |
| `MODEL` | модель для всех стадий; она же в поле `model` submission | `gemini-3.5-flash-lite` |
| `REASONING_EFFORT` | `low\|medium\|high` или пусто | — |
| `LLM_SEED` | seed запроса или `none`; слой Gemini поля не принимает | `none` |
| `RPM` | запросов в минуту | `14` |
| `RPH` | запросов в час; `0` — окна нет | `0` |
| `MAX_CONCURRENCY` | параллельных вызовов | `4` |
| `TEAM` | поле `team` в submission | `halyk-agent` |
| `CONTACT_EMAIL` | поле `contact_email` | — |
| `DATA_DIR` | каталог датасета | `./data` |
| `OUT_DIR` | результаты | `./out` |
| `ARTIFACTS_DIR` | артефакты стадий | `./artifacts` |
| `CACHE_DIR` | кэш страниц | `./.cache` |
| `DB` | путь к SQLite-стору | `./.cache/halyk.db` |
| `LOG_DIR` | журналы и дампы вызовов | `./logs` |
| `LOG_LEVEL` | `debug\|info\|warn\|error` — порог для stderr; в файл пишется всё | `info` |

Планировщик держит оба окна сразу и берёт больший интервал: у Gemini часового окна нет
(`RPH=0`), темп задаёт `RPM`. Провайдер меняется тремя переменными — `LLM_API_KEY`,
`LLM_BASE_URL`, `MODEL` — без правки кода.

`config/fx.yaml` задаёт запасные курсы валют на случай, когда документы заёмщика курса не
раскрывают. Курс из факт-базы всегда главнее; применение запасного попадает в журнал.

## Что получается на выходе

```
out/
├── submission.json    ответ по форме шаблона — то, что оценивается
├── audit.jsonl        по вердикту на строку: трасса, вклад, уверенность
├── review.md          ячейки на ручную проверку
└── report.pdf         человекочитаемое досье на весь прогон

artifacts/
├── doc_index.json     каждый документ: тип, заёмщик, чем распознан
├── specs/<scn>.json   спецификации ковенантов
├── facts/<scn>.json   факт-базы
└── labels/<scn>.json  размеченный леджер; _patterns.json — шаблоны описаний на весь корпус

.cache/halyk.db        стор: документы, страницы, транзакции, артефакты, вердикты, кэш ответов
.cache/png/            отрендеренные страницы для OCR
logs/run.log           структурированный журнал прогона, все уровни
logs/llm/              сырые ответы модели
```

`report.pdf` собирается только из `out/submission.json` и стора, модель не вызывается, поэтому
отчёт можно перерисовывать сколько угодно. Внутри: сводка, таблица всех ячеек, по каждому
заёмщику — счета, связанные стороны, курсы, корректировки, итоги леджера, а на каждый пункт
статус, `actual`, выражение с порогом и периодом, термы, evidence с расшифровкой транзакции,
уверенность, реплика критика, цитата договора и трасса вычисления. Заканчивается приложением
«Cells to check by hand»: ячейки с низкой уверенностью и те, с которыми не согласился критик.
Текстовый слой в PDF есть — по отчёту работают поиск и `pdftotext`.

```bash
bin/halyk-agent report --only P1,B4          # только эти заёмщики
bin/halyk-agent report --file /tmp/p1.pdf    # другой путь
```

## Файловая структура

```
cmd/halyk-agent/     точка входа
internal/
├── cli/             команды cobra, журналирование, порядок стадий в run
├── config/          .env, пути, лимиты, проверка комплектности датасета
├── domain/          типы предметной области; на другие пакеты не опирается
├── store/           SQLite: документы, страницы, транзакции, артефакты, вердикты, кэш LLM
├── llm/             клиент провайдера: лимит запросов, повторы, кэш, квотный шлюз
├── agents/          промпты и разбор ответов: триаж, ковенанты, факты, разметка, критики
├── ingest/          фаза 0: CSV, шаблон, PDF → текст
├── index/           фаза 1: индекс документов и привязка к заёмщику
├── covenants/       фаза 2: разбор договора на статьи и пункты, извлечение спек
├── facts/           фаза 3: факт-база заёмщика
├── classify/        фаза 4: шаблоны описаний, keyword-правила, метки строк
├── engine/          фазы 5-6: вычислитель выражений, термы, вердикт, evidence
├── evaluate/        оркестровка движка, критик по ячейкам, review.md
├── submit/          заполнение шаблона, валидация, локальный скоринг
├── report/          PDF-отчёт
└── stability/       повторные прогоны модели и сравнение ячеек
config/fx.yaml       запасные курсы валют
data/                датасет (в .gitignore)
```

## Что делает каждый пакет

**`domain`** — словарь проекта. Транзакция, леджер, документ, шаблон, ковенантная спецификация
с термами, факт-база, метка строки, вердикт, таксономия категорий и перевод формулировок
отчётности в категорию. Ни от чего внутри проекта не зависит; всё остальное зависит от него.

**`config`** — чтение `.env` и окружения, пути к файлам датасета и результатов, лимиты запросов,
проверка ключа и комплектности датасета.

**`store`** — единственное хранилище состояния поверх SQLite: документы и их страницы с
полнотекстовым поиском, транзакции, артефакты стадий по ключу «вид + идентификатор», вердикты и
кэш ответов модели по хешу промпта. Здесь же `RequireArtifact`, который на отсутствующий артефакт
отвечает ошибкой с именем стадии, которую надо прогнать.

**`llm`** — клиент OpenAI-совместимого `/chat/completions`: лимит запросов, повторы, разбор
ошибок провайдера, кэширование, дамп вызовов. Здесь же квотный шлюз: после первого ответа
«суточная квота исчерпана» сеть больше не трогается, а закэшированное продолжает отдаваться.

**`agents`** — всё, что разговаривает с моделью: инструкции и схемы ответов для триажа,
извлечения ковенантов, факт-базы, разметки строк, а также критиков спеки и вердикта. Разбор
JSON, нормализация значений и одна повторная попытка с ремонтным промптом, когда ответ не
разобрался. Ни один агент не возвращает вердикт.

**`ingest`** — фаза 0, без модели. Разбирает CSV леджера и шаблон, вытаскивает текст каждого PDF
через `pdftotext`, а страницы без текстового слоя рендерит и распознаёт `tesseract` в
`rus+eng+kaz`. Повторный запуск пропускает документы с прежним SHA-256.

**`index`** — фаза 1. Детерминированный пре-скан ищет в тексте `account_id`, номера пунктов и
баннеры об утрате силы; модель называет тип документа и субъекта. Дальше документ привязывается
к заёмщику: сперва по `account_id` через леджер, затем по названию компании, затем голосованием
по контрагентам, встречающимся только у одного заёмщика. Отдельно отмечаются недействующие
редакции и консолидированная отчётность группы. `CheckCoverage` говорит, у кого не нашлось
действующего договора со всеми запрошенными пунктами.

**`covenants`** — фаза 2. Режет договор на статьи и пункты регулярными выражениями, собирает
контекст (текст пункта, статью целиком, определения, изменения) и просит модель превратить пункт
в исполняемую спецификацию, после чего критик перечитывает её против исходного текста. Заёмщик,
у которого договор не читается или в нём нет нужных пунктов, выпадает поодиночке — остальные
считаются.

**`facts`** — фаза 3. Собирает по заёмщику всё, что меняет то, что видит ковенант: корректировки
аудитора (реклассификации, отсечения периода, отклонённые, раскрытые суммы, восстановленные
суммы пустых строк), таблицу владения с порогом связанности, покрытие обеспечением дочерних
организаций, курсы валют и движение основных средств группы.

**`classify`** — фаза 4. Сводит описания строк к шаблонам, размечает каждый шаблон
keyword-правилами и моделью, расхождения отправляет на эскалацию. Затем ставит метки на строки
заёмщика, помечает связанные стороны и привязывает корректировки аудитора к строкам: по
`txn_id`, затем по контрагенту, затем по сумме вместе с похожим названием. Прогон, не дошедший
до модели, не затирает разметку, которую модель уже подтвердила.

**`engine`** — фазы 5–6, чистые вычисления без ввода-вывода и без модели. Свой вычислитель
арифметических выражений на `decimal` с `max`/`min`, скобками и явной ошибкой деления на ноль.
Считает термы по видам (строка отчётности, примечание, категория леджера, платежи связанным
сторонам, консолидированный показатель, константа), применяет реклассификации, проверяет триггер
и carve-out'ы, сравнивает с порогом. Evidence выбирается leave-one-out: транзакция, без которой
вердикт меняется.

**`evaluate`** — оркестровка: собирает входы стадий, приводит суммы к долларам по курсам, гоняет
движок по ячейкам, отдаёт готовую ячейку критику и пишет `out/review.md`. Критик статус не
меняет — только опускает уверенность и помечает ячейку.

**`submit`** — заполнение шаблона на месте через `sjson`, побайтово, а не пересборкой JSON из
map: ключи нельзя переименовать, потерять или добавить, а номера пунктов вроде `6.1`
экранируются, иначе точка была бы принята за разделитель пути. Плюс `audit.jsonl` с трассами,
валидация результата против шаблона и локальный скоринг против `ground_truth.json`.

**`report`** — сборка `out/report.pdf` через `fpdf` со вшитыми шрифтами DejaVu. Источник ответов
— сам submission, источник обоснований — стор.

**`stability`** — переизвлекает спеки, факт-базы и метки в отдельном namespace с подсоленным
кэшем, считает ячейки заново и сравнивает с сохранёнными. Отвечает на вопрос, сколько ответов
держится при повторном прогоне модели. Submission не трогает.

**`cli`** — команды, флаги, журналирование, порядок стадий в `run` и политика деградации.

## Как ведёт себя при сбоях

Молчаливых заглушек нет: всё, что не посчиталось, названо в отчёте стадии поимённо. Но и «упасть
и ничего не отдать» стадия не может — по условию задачи пустая ячейка стоит ровно столько же,
сколько неверная, поэтому `run` доводит прогон до submission при любом исходе. Критична только
`ingest` — без леджера и шаблона собирать нечего.

| Стадия | Сбой одного элемента | Полный отказ стадии |
|---|---|---|
| `triage` | документ остаётся нераспознанным | дальнейшие модельные стадии пропускаются |
| `covenants` | заёмщик без спек, его ячейки на baseline | то же |
| `facts` | заёмщик без факт-базы | то же |
| `classify` | батч размечается keyword-правилами | то же |
| `evaluate` | ячейка остаётся на baseline | submission из одних заглушек |

Деградация не имеет права ухудшать то, что уже посчитано. Батч `classify`, не дошедший до
модели, отдаёт шаблону разметку keyword-правила — но если на этот шаблон в сторе лежит ответ
модели, прогон оставляет прежний и говорит об этом строкой `KEPT` и ненулевым кодом возврата.
Обратной блокировки нет: свежий ответ модели всегда заменяет прежний.

## Чего код не знает про этот корпус

Агент должен работать на документах, которых он не видел, поэтому действует одно правило:
**признак решает, только если он объявлен артефактом.** Следствия:

- Ветку вычисления терма выбирает `kind` спеки, а не формулировка пункта. Исключение одно —
  EBITDA: это определение показателя, и оно распознаётся словарём синонимов.
- Сужение до дочерних организаций вне залога — поле `entity_scope`, которое объявляет модель.
  Кода, читающего статус контрагента из текста пункта, в проекте нет.
- Категория строки отчётности определяется самым длинным совпадением в таблице псевдонимов, а не
  первым: перестановка таблицы ничего не меняет.
- Keyword-правила разметки построены как «словарь категории + словарь возврата», а не как список
  описаний из этого реестра. На них есть тест, обязанный проходить на формулировках, которых в
  корпусе нет.
- Корректировка аудитора привязывается по `txn_id`, затем по контрагенту, затем по сумме — но
  сумма принимается только вместе с похожим названием, а `txn_id`, противоречащий контрагенту и
  сумме, отвергается.
- Единственная величина, взятая из корпуса, лежит в `config/fx.yaml`, с провенансом в
  комментарии, и её применение попадает в журнал.
- В промптах нет значений из датасета: числа в примерах формата выдуманы и проверены на
  отсутствие в корпусе.

## Внешние зависимости

`cobra` — команды, `modernc.org/sqlite` — стор без cgo, `shopspring/decimal` — деньги,
`tidwall/gjson` и `sjson` — заполнение шаблона с сохранением порядка ключей, `go-pdf/fpdf` —
отчёт, `golang.org/x/sync` — ограничение параллелизма.

## Дисклеймер датасета

Все данные набора вымышлены: заёмщики, контрагенты, аудиторы, договоры и все приведённые числа
созданы для конкурса. Ни один документ не отражает реального клиента, договор или операцию
какой-либо организации.
